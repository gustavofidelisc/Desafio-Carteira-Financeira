# Exemplo de variáveis de ambiente para configuração do Prisma e autenticação

# URL de conexão com o banco de dados (substituir pelos valores reais)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Chave secreta para geração de tokens JWT (substituir por um valor seguro)
JWT_SECRET="SUA_CHAVE_SECRETA_AQUI"